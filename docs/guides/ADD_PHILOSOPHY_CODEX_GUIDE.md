# 迷雾学派 哲学辞典 词条补充与撰写指南

这份文档是为 AI 提供的标准规范，用于指导如何为“迷雾学派”哲学辞典系统（Philosophy Codex）补充新的词条数据。

---

## 1. 架构与文件位置

词条数据的增删修改分为以下两个核心步骤：

1. **词条的“户籍”管理（索引注册）**：
   - **位置**：`/data/codex/philosophy_***.ts`（例如 `philosophy_refined.ts`）。
   - **作用**：定义词条是否存在、ID 标识、以及在列表页展示的简短描述。如果需要合并或重命名词条，必须先在此处修改。
2. **内容的“内脏”填充（详细文本）**：
   - **位置**：`/public/data/codex/[哲学家]/details/[ID].json`。
   - **作用**：存放 1500+1200+1200 格式的硬核长文本。文件名必须与索引中的 ID 严格对应。

### 第一步：注册词条索引 (TypeScript)
你需要在核心数据文件中先“注册”这个词条。
- **文件位置**：`/data/codex/philosophy_***.ts` （例如 `philosophy_hegel.ts`、`lacan_dictionary.ts` 等，根据具体哲学家选择）。
- **字段要求**：
  为 `concepts` 数组添加一个新的对象，必须包含：
  ```typescript
  {
      id: "独特的词条英文ID", // 例如: "h_aufhebung"
      name: "中文词条名", // 例如: "扬弃"
      enName: "原文/英文词条名", // 例如: "Aufhebung / Sublation"
      category: "所属主分类", 
      shortDef: "一到两句话的精简定义（会在卡片与列表上显示）",
      author: "提出者（选填）",
      source: "原文出处（选填）",
      tags: ["TAG1", "TAG2"], // 标签（选填）
      related: ["关联词条1", "关联词条2"] // 关联性提示（选填）
  }
  ```

### 第二步：撰写详情 JSON 数据
为了避免打包体积过大，长篇幅の正文阅读材料存储为独立的 JSON 文件，通过网络异步加载。
- **文件位置**：`/public/data/codex/[哲学家英文小写名]/details/[词条ID].json`
  *(注意：文件名必须与你在第一步中定义的 `id` 完全一致，加上 `.json` 后缀。例如 `/public/data/codex/hegel/details/h_aufhebung.json`)*。

- **JSON 数据格式**：
  文件必须是一个基础的三段式 JSON 对象。文本内容支持 **Markdown 排版**（可使用标题 `#`、加粗 `**`、列表 `-` 等）。
  ```json
  {
    "definition": "### 1. 核心定义与渊源...\n在这里写第一部分...",
    "analogy": "### 1. 具象类比...\n在这里写第二部分...",
    "application": "### 1. 叙事引擎部署指示...\n在这里写第三部分..."
  }
  ```

---

## 2. 撰写风格与字数要求

**这是一部高度专业、具有极强理论深度的黑核哲学辞典，同时也是一个为了“爱欲迷宫”叙事与电影创作服务的设定集。**撰写单个词条 JSON 时，总字数（含三部分）应保持在 **4000 字左右**，以确保分析的透彻度与阅读的压迫感。

请严格按照以下三段式结构（JSON的三个字段）输出该词条：`definition`, `analogy`, `application`。在具体撰写前，**AI必须要先查阅以下核心架构文档与代码参考**，以确保与迷雾学派的体系完全吻合：
- `ADD_PHILOSOPHY_CODEX_GUIDE.md` (本文档，了解结构与字数要求)
- `data/codex/philosophy_refined.ts` 和 `data/codex/lacan_dictionary.ts` (查看词条分类、索引架构与已有哲学体系)
- `data/narrative_engine.ts` 和 `backend/data/whitepaper_data.ts` (深入理解“爱欲视差”、剧作公式组合与核心理论参数)

每一部分的具体要求如下（请严格遵循）：

### Part 1：核心定义 (definition) 
**重点：学术性、原典对读、辩证法深潜。**
*要求：不少于 1500 字。这部分绝不能是百科全书式的简单说教。*
- **哲学史坐标**：将该概念置于哲学史的演变中（例如它超越了之前谁的哪种观点），解释它为什么在思想史上是个“重磅炸弹”。不做通俗的敷衍解释，必须触及概念在原著（如《精神现象学》《逻辑学》等）中的本质。
- **底层逻辑剖析**：剥去学术黑话的外壳，用极具穿透力和推演性的语言剖析其运作机制（如：否定性、异化、辩证运动等）。展现该概念的前后矛盾与推演过程。
- **语言风格**：庄重、深邃、带有本体论的悲剧感或史诗感（如同描述一个“饱经风霜的生命体”一般）。语气要极度客观、冷峻、如同一份发黄的绝密学术档案。

### Part 2：拓扑类比与案例 (analogy)
**重点：降维打击、荒诞与日常的结合、拓扑学隐喻。**
*要求：不少于 1200 字。用最具象、最致命的案例击穿抽象概念。必须包含以下至少两个维度的深刻案例：*
- **通俗拓扑/奇观类比**：用物理学、拓扑学（如莫比乌斯环、克莱因瓶）、生物学变异（如毛毛虫到蝴蝶、细胞吞噬）或者自然科学现象作第一层隐喻。
- **通俗文化/时事/生活类比（核心）**：用最贴近现代人日常生活的痛点（如：婚姻危机、职场异化、消费主义陷阱、地缘政治），或者大家熟知的**经典电影文本**（如《黑客帝国》《楚门的世界》《搏击俱乐部》）、**文学名著**去投射这个哲学概念。让人看完产生一种恍然大悟的悚然感：“原来日常和通俗文本的背后是这样的哲学规律”。

### Part 3：迷雾学派创作理论应用 (application)
**重点：结构映射、电影剧本操作指南、爱欲公式对接。**
*要求：不少于 1200 字。标题统一采用 `### 迷雾学派创作理论应用`。这一段必须把它当做给导演、编剧和剧本策划写的“操作指南”！*
- **对接“爱欲迷宫”核心公式**：明确指出这个哲学概念如何印证或指导了我们的核心议题（例如，它如何体现了欲望的错位、视差之见、或主体在符号界的塌陷），并与我们的创作理论深度结合。
- **剧本/电影叙事降维**：将概念翻译成具体的**剧作元素**。比如它应该用在三幕剧的什么位置？它是用来塑造反派的执念，还是用来构建中点（Mid-point）的灵魂黑夜？或是用来设计一种“打破第四面墙”的叙事诡计？不可泛泛而谈故事怎么写，要写出具有极高执行力的操作指南。
- **映射具体理论参数**：指出它体现在或影响了我们的哪个理论参数或系统机制（如：M1主体遭遇、M3幻想、M5驱力、以及 Eros Intensity 的波动公式等）。给出具体的**创作指示 (Directive)**，明确告诉创作者“在这个桥段里，角色绝不能怎么做，必须怎么做”。

---

## 3. 标准化执行流程（AI 工作流）

当你收到用户要求“帮我补充 [XXX] 词条”时，请执行以下步骤：
1. **分析并确认归属**：确认这个概念属于哪位学派大师。
2. **在 TypeScript 中注册节点**：编辑对应的 `data/codex/philosophy_***.ts` 文件，写入并完善包含 `id`、`shortDef`、`tags` 等字段的卡片节点。
3. **编写 4500 字详情 JSON**：利用工具创建 `/public/data/codex/[哲学家]/details/[id].json` 文件，并确保其格式为合法的 JSON。由于含有大量换行符和引号，建议小心处理 JSON 转义，或者使用文件写入工具直接生成。
4. **输出报告**：告诉用户索引已插入，详情页面 JSON 已生成，提示其可以刷新页面并在左侧边栏查看效果。
