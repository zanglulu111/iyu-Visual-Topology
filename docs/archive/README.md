# 归档说明

这里保存不属于当前运行主线、但仍有历史参考价值的材料。

- `legacy-web/codex-web/`：旧静态网页原型，当前 Vite 应用不依赖它。
- `claude-plans/`：旧 Claude 计划文档。
- `test-engine.ts`：一次性引擎测试脚本，已从根目录移出。
- `mist-history/`：旧 mist 理论树与迁移记录，保留历史语义，不作为 active theory 入口。

归档内容默认不作为新功能依赖；若要重新启用，应先迁回正式源码目录并补充当前类型与构建验证。
